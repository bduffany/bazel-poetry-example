# Bazel poetry example

This repo shows how to build a poetry-managed project using Bazel.

Run `./run.sh` to build and run the example binary, which includes
a dependency on joblib that is installed by poetry.
